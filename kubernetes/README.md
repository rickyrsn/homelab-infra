# Kubernetes

Keep runtime manifests here.

- `bootstrap/`
  One-time cluster bring-up helpers.
- `platform/`
  Shared services like ingress, storage, monitoring, and certificates.
- `apps/`
  Personal workloads deployed after the platform is stable.
